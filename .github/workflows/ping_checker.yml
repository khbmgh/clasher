name: Check and Clean Proxies

on:
  workflow_run:
    workflows: ["Update All Proxies"] # ⚠️ نکته مهم: این نام باید دقیقاً نام اکشن اول شما باشد
    types:
      - completed
  workflow_dispatch: # فعال کردن دکمه اجرای دستی

permissions:
  contents: write

jobs:
  ping-and-update:
    runs-on: ubuntu-latest
    # این شرط بررسی می‌کند که اکشن اول حتماً موفقیت‌آمیز بوده باشد (یا اینکه دستی اجرا کرده باشید)
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    steps:
      - name: Wait 15 minutes
        # اگر اکشن به صورت خودکار بعد از اکشن اول اجرا شده بود، 15 دقیقه (900 ثانیه) صبر کند
        if: ${{ github.event_name == 'workflow_run' }}
        run: sleep 900

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruamel.yaml

      - name: Run Ping Script
        run: python ping_proxies.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # اضافه کردن هر دو فایل به استیج گیت
          git add all.yaml hysteria2.yaml
          
          # در صورت وجود تغییرات، کامیت و پوش انجام می‌شود
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-removed dead proxy servers 🤖" && git push)
